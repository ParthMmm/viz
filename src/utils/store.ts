import { atom, useAtom } from 'jotai';

export const optionAtom = atom('albums');
export const userAtom = atom('parth_m');
export const introAtom = atom(true);
export const loadingAtom = atom(false);
